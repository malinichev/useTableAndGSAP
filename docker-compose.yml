version: '3'
services: 
    useTableAndGSAP:
        container_name: useTableAndGSAP
        restart: always
        build: .
        ports:
            - "5555:80"
            - "443:443"
        volumes:
            - /etc/letsencrypt/:/etc/letsencrypt/
       